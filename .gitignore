# Xcode (project.yml → xcodegen 으로 생성)
CoNo.xcodeproj/
build/
DerivedData/
xcuserdata/
*.xcuserstate
.DS_Store
.env*.local

# 보컬 분리 모델 (scripts/fetch-models.sh 로 받음)
Models/

# 개인 서명 설정 (Support/Signing.xcconfig 가 있으면 읽는다)
Support/Signing.local.xcconfig
