# 결정 기록

## 2026-09-25 — macOS 네이티브 (Swift) 로 시작
- 후보: A) macOS 네이티브 / B) Tauri + Python 추론 / C) Chrome 확장
- 선택: **A**. Core Audio process tap(macOS 14.2+)이 앱별 캡처와 원본 음소거(`mutedWhenTapped`)를 공식 API 로 제공. iOS 는 원천 불가, Android 는 Spotify/Apple Music 이 캡처를 막아둔다.

## 2026-09-25 — ~~애그리게이트 한 개에서 입출력을 같이 처리~~ (아래 "캡처·재생 분리"로 대체)
- 탭과 기본 출력 장치를 한 애그리게이트로 묶고 한 IOProc 에서 입력(탭)을 받고 출력에 씀.
- 이유: 별도 출력 엔진을 쓰면 클럭이 두 개가 되어 드리프트 보정이 필요. 한 IOProc 이면 같은 클럭.
- 한계: 시작 후 기본 출력 장치를 바꿔도 따라가지 않는다 (재시작 필요).

## 2026-09-25 — 지연 버퍼(프리롤) 방식
- 초저지연 분리 모델 대신 N초 늦게 재생해서 그 시간을 분석에 쓴다. 원본은 음소거되므로 사용자는 지연을 느끼지 않는다.
- 부수 효과: 앞으로 부를 음정을 N초 미리 보여줄 수 있다.
- 비용: 곡 시작 시 N초 무음, 탐색·건너뛰기 반응이 N초 늦음.

## 2026-09-25 — 앱 그룹핑에 responsible pid (private SPI) 사용
- 브라우저·Electron 앱은 헬퍼 프로세스가 소리를 낸다 (Safari → `com.apple.WebKit.GPU`).
- `responsibility_get_pid_responsible_for_pid` 를 dlsym 으로 찾고, 없으면 번들 ID 접두어 매칭으로 폴백.
- App Store 배포 시 재검토 필요.

## 2026-09-25 — 시스템 전체 캡처는 자기 제외 실패 시 거부 (fail-closed)
- CoNo 출력이 다시 탭으로 들어가면 무한 에코. 자기 프로세스 오브젝트를 못 찾으면 시작하지 않는다.

## 2026-09-25 — ~~로컬 PoC 는 ad-hoc 서명~~ (아래 "회사 인증서 서명"으로 대체)
- 팀 ID 없이 바로 빌드 가능. 대신 빌드마다 서명이 바뀌어 오디오 캡처 권한을 다시 물을 수 있다.

## 2026-09-25 — 검증: Apple Music (FairPlay) 캡처 동작 확인
- Apple Music 앱을 process tap 으로 캡처 + 원본 음소거 + 지연 재생까지 정상 동작 (사용자 실측).
- 타당성 조사의 1순위 리스크("DRM 재생 소리가 캡처되는가")가 Apple Music 에서는 해소됨. 멜론·YouTube Music(Chrome) 은 미검증.

## 2026-09-25 — 보컬 분리: ONNX Runtime + UVR MDX-Net Karaoke 2
- 후보: A) ONNX Runtime + MDX-Net / B) Demucs → CoreML 변환 / C) Python 보조 프로세스
- 선택: **A**. 네이티브 단일 앱, 모델 교체 쉬움, Karaoke 모델이 코러스를 반주에 남겨 노래방 느낌에 가깝다.
- 모델 파일은 커밋하지 않고 `scripts/fetch-models.sh` 로 받는다 (UVR 해시로 설정값 검증). 가중치 라이선스 미명시 → 배포 전 확인 필요.

## 2026-09-25 — 슬라이딩 윈도우 스트리밍 (step / rightContext / fade)
- 모델 창(5.9초)이 고정이라 매 step 마다 창 전체를 다시 분리하고 뒤쪽 일부만 쓴다. 연산은 늘지만 지연을 초 단위로 조절 가능.
- 기본값 step 1.0초, rightContext 1.0초, fade 2048 샘플. 권장 프리롤 = rightContext + step + 추론×1.5 + 0.5초.

## 2026-09-25 — CoreML: RequireStaticInputShapes = "0"
- UVR 모델은 배치 차원이 기호(`batch_size`)라 "1" 이면 CoreML 이 178개 노드를 전부 거부 → 전부 CPU(약 14초/창).
- ObjC API 에 free dimension override 가 없어 동적 형태 허용으로 해결 → 전 노드 CoreML, 약 150ms/창.
- MLComputeUnits 값은 대문자 `ALL` (헤더 주석의 "All" 은 거부됨).

## 2026-09-25 — 검증: AI 분리 실사용 품질 확인
- Apple Music 곡으로 실시간 분리 재생 — 사용자 평가 "거의 완벽하게 분리". 기본값(step 1.0초, rightContext 1.0초, CoreML 자동)으로 충분.

## 2026-09-25 — 음정 검출: SwiftF0 (모델 변환본 커밋)
- 후보: A) SwiftF0 ONNX / B) YIN 직접 구현 → **A**. 벤치마크 1위, 135 KB, MIT, 기존 ONNX Runtime 재사용, 마이크 채점에도 재사용 예정.
- ORT ObjC API 에 double 텐서 타입이 없어 원본 pitch(double) 출력을 못 읽음 → `scripts/convert_swiftf0.py` 로 Cast(float) 를 붙인 변환본을 `CoNo/Resources/swift_f0.onnx` 에 커밋 (원본 md5 검증).
- 모델 준비 시 220 Hz 사인파 자가진단 → 220.0 Hz 확인.

## 2026-09-25 — 음정 바 시간축 = 출력 스트림 초
- 보컬과 반주가 같은 출력 스트림에서 나오므로, 음정 프레임과 재생 위치를 둘 다 출력 스트림 기준으로 세면 rightContext·프리롤 오프셋 계산이 필요 없다.
- 캡처 시각이 필요한 가사 싱크에서만 스트림 오프셋(rightContext)을 쓴다.

## 2026-09-25 — 캡처·재생 분리 (샘플레이트 불일치 틱 해결)
- 증상: 스피커 96 kHz, Apple Music(탭) 48 kHz 에서 패스스루도 불규칙한 틱. 언더런 0. 스피커를 48 kHz 로 맞추면 사라짐 → 원인 확정.
- 원인: 탭 포맷은 장치 레이트가 아니라 앱 믹스 레이트를 따른다. 출력 장치 + 탭 을 한 애그리게이트에 넣으면 HAL 이 서브탭 드리프트 보정으로 2배 변환 → 불연속.
- 후보: A) 캡처·재생 분리 + 자체 변환 / B) 실행 중 장치 레이트 변경 / C) 경고만 → **A** (시스템 설정 불변, 어떤 레이트든 동작).
- 구현: 탭 전용 애그리게이트(TapAutoStart false, 드리프트 보정 off) + AVAudioEngine 소스 노드 재생 + 워커에서 AVAudioConverter.
  캡처·재생 콜백이 다른 스레드가 되어 IO 진단(사이클 건너뜀·콜백 최대 시간)도 각각.
- 같이 해결: 장치 시작을 백그라운드로 (권한 창 대기 중 UI 멈춤), 출력 장치 변경 시 정지·안내.
- 상태: **검증 완료** — 스피커 96 kHz · 탭 48 kHz 에서 틱 없음 (사용자 확인).

## 2026-09-25 — 음정 바가 멈춰 보이던 버그
- TimelineView 안 Canvas 가 틱 시각(date)을 안 써서 SwiftUI 가 매 틱 그리기를 건너뜀 (창 리사이즈 때만 갱신). 렌더러에 date 전달로 해결, 사용자 확인.

## 2026-09-25 — 키 조절: Apple AVAudioUnitTimePitch 유지
- ±6 반음에서 음질 문제 없음 (사용자 확인). 원키는 bypass.
- 교체 검토 신호: 보컬이 섞인 소리에 키 적용(포먼트 보존 필요) / 타 OS 이식 / 큰 폭 음질 불만 → 그때는 라이선스 제약 없는 Signalsmith Stretch(MIT) 우선, Rubber Band(GPL/상용) 차선.

## 2026-09-25 — 마이크 채점: 이어폰 전제 + 옥타브 무시 기본 + 프레임 단위 채점
- 에코: 스피커 출력이면 반주가 마이크로 들어가 채점 오염 → 이어폰 전제, 내장 스피커 출력·블루투스 마이크(통화 모드)면 경고. Apple 에코 제거·자체 AEC 는 후속 실험.
- 옥타브: 기본은 음이름만 맞으면 정답(오차를 ±6 반음으로 접음), 엄격 모드 옵션.
- 채점 단위: 음표 아닌 **16 ms 프레임**. 원곡 유성 프레임마다 ±0.1 초 안의 내 음정이 목표(반음 반올림 + 키 조절)의 ±0.5 반음 안인지. 음표 식별 일치 문제를 피한다. 막대 색은 소속 프레임 적중률.
- 시간 맞춤: 마이크 버퍼 호스트 시각 → 재생 시계(출력 스트림 초) − 키 조절 지연 − 화면 싱크 보정 = "그때 귀에 들리던 위치".

## 2026-09-25 — 가사 L1: Apple Music 스크립트 + LRCLIB + 캡처 시계 앵커
- 곡 식별: macOS 15.4+ 는 MediaRemote(지금 재생 중)를 서드파티에 막음 → 음악 앱 공식 AppleScript(제목·아티스트·앨범·길이·재생 위치). 다른 앱은 L2(소리 기반 자동 싱크)·L4(ShazamKit, 유료 개발자 계정 있음) 예정.
- 가사: LRCLIB 실측 12곡 — 한·일 인기곡 대부분 싱크 가사 있음, 아티스트 표기 민감("아이유" 0건 / "IU" 있음), 로마자 항목 혼재, 단어 타이밍은 사실상 없음, 간헐 503. → 표기 바꿔 재검색 + 길이 ±3초 + 원문 문자 우선 + 재시도 + 캐시.
- 싱크: 폴링마다 (재생 위치, 그 순간의 캡처 스트림 시각) 앵커. 들리는 소리의 캡처 시각에 해당하는 앵커로 곡 위치 계산 → 들린 뒤에 일어난 일시정지·되감기·곡 변경이 현재 표시를 흔들지 않음.
  캡처 시각 = 들리는 출력 스트림 위치 − (AI 모드면 rightContext).
- 표시: 2줄(현재 줄 색칠 + 다음 줄), 색칠은 줄 길이 비례 근사 — L3 에서 음표 시작점 기반 음절 단위로.
- 저작권: 개인용 PoC 로 LRCLIB 사용. 배포 시 정식 라이선스(Musixmatch·LyricFind 등) 검토.
- 마이크 채점(PoC ④)은 설계만 기록하고 보류 — 가사 먼저.

## 2026-09-25 — 회사(AIB Inc.) 개발 인증서로 서명
- 유료 개발자 계정. 개인 팀(2QPC5T633N)·회사 팀(9BF5ZBVYYF) 중 **회사 팀**으로 결정 (사용자).
- 특수 entitlement 가 없어 수동 서명 + 프로필 없음. designated requirement = 앱 ID + 인증서 CN → 빌드마다 TCC 재질문 없음.
- ShazamKit(L4) 도입 시 App ID 에 ShazamKit 서비스 활성화 + Automatic 서명·프로필로 전환 필요.
